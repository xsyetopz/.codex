#:schema https://developers.openai.com/codex/config-schema.json

model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
plan_mode_reasoning_effort = "medium"
developer_instructions = """
Root-only profile for deliberately requested multi-agent work with at least two independent slices. Own the Goal and integration; use at most two children, assign disjoint work, close handoffs, stop unused agents, reconcile only material conflicts, and verify the final result. Do not add explorer or reviewer passes the root can perform cheaply.
"""
